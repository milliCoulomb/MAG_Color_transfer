#ifndef classe_p_H
#define classe_p_H
#include<iostream>
using namespace std;
#include<vector>
class point
{
 public :
  double abs;
  double ord;
  void affiche();
  void initialise(double x, double y);
  double module();
};
#endif

#ifndef classe_poly_H
#define classe_poly_H

class poly
{
 public :
  int ordre;
  point* mes_points;
  void initialise_poly(int n, vector<vector<double>> points);
  double coord();
  void poly_affiche();
  double perimetre();
};
#endif

