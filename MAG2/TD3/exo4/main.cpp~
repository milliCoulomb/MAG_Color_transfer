#include<iostream>
#include<ctime>
#include<cstdlib>
using namespace std;
#include"classe_hasard.h"
int main() {
  srand(time(0));
  hasard hA(10, 8);
  hasard hB(10, 5);
  hA.affiche();
  if (hA.comparaison(hA)) {
    cout << "Wouah ça marche" <<endl;
  }
  if (! hB.comparaison(hA)) {
    cout << "Pas de chance" << endl;
  }
  return 0;
}
