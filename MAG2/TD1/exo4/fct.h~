#include<iostream>
#include<cmath>
#include"fct.h"
using namespace std;
int main() {
  double a = 1;
  double b = 1;
  double & res_a = a;
  double & res_b = b;
  expo_par_ref(a,b,res_a,res_b);
  cout << res_a << " " << res_b << endl;
  return 0;
}
